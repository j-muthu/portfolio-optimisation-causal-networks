# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# OS
.DS_Store

# Claude local settings
.claude/settings.local.json

# Reference PDFs — papers kept locally, not version-controlled
*.pdf

# Data cache (downloaded prices / Wikipedia tables — regenerable)
cache/

# Rodriguez-Dominguez HSP reference dataset — published-paper supplements
# kept locally for tests/test_hsp_reference.py but not redistributed.
HSP/

# Experiment artefacts: pickled results are large; CSVs/PNGs/logs are tracked
results/**/*.pkl
# Per-run feedback state — regenerable, and the exploratory runs are noise.
results/**/utility.parquet

# Local secrets (API keys / WRDS credentials) — never commit
.env
*.env.local

old_notes/
interim_report/interim_report.*
!interim_report/interim_report.tex

interim_report/interim_report_template.*
!interim_report/interim_report_template.tex

varlingam_docs/

latex_template/*

final_report/*

# final_report_lean: track LaTeX source + figures + the built PDF, but not the
# LaTeX build intermediates.
final_report_lean/main.aux
final_report_lean/main.bbl
final_report_lean/main.bcf
final_report_lean/main.blg
final_report_lean/main.fdb_latexmk
final_report_lean/main.fls
final_report_lean/main.log
final_report_lean/main.out
final_report_lean/main.run.xml
final_report_lean/main.toc