\documentclass[12pt, a4paper]{report}

% Custom Computing group formatting
\usepackage{ccrep}

% Additional packages
\usepackage{booktabs}
\usepackage{array}
\usepackage{enumitem}

% ------------------------------------------------------------------
% Allocator names. The stem is where the ordering comes from, the
% suffix is what recursive bisection splits on, so each name names a
% cell of the crossing in Table~\ref{tab:crossing}. These are display
% names only: the code, caches and CSVs keep their own tags, and the
% mapping is mirrored in scripts/plot_phase_ii_figures.py (DISPLAY) --
% keep the two in step.
%   \HRP        <- CORR-HRP   textbook correlation HRP (graph-blind anchor)
%   \skelsamp   <- D0         skeleton ordering, sample covariance
%   \skelalt    <- D0s        second symmetrisation of the skeleton
%   \skelsem    <- D1         skeleton ordering, Sigma_SEM
%   \orientsamp <- D2         topological ordering, sample covariance
%   \orientsem  <- D2s        topological ordering, Sigma_SEM
% NB \HRP is the *allocator*; plain "HRP" in prose is the algorithm.
% ------------------------------------------------------------------
\newcommand{\HRP}{\textsc{hrp}}
\newcommand{\skelsamp}{\textsc{skel-samp}}
\newcommand{\skelalt}{\textsc{skel-alt-samp}}
\newcommand{\skelsem}{\textsc{skel-sem}}
\newcommand{\orientsamp}{\textsc{orient-samp}}
\newcommand{\orientsem}{\textsc{orient-sem}}
\newcommand{\orientany}{\textsc{orient-*}}

% Referencing (Vancouver style, mandated)
\addbibresource{references.bib}

% Robust-stats macros (PSR/DSR/SPA/MCS, seed audit, measurement problem),
% generated over the unified trial universe by
%   python -m scripts.robust_stats --phase-ii
% The committed file carries the authoritative values.
\input{_generated/robust_stats}

% ------------------------------------------------------------------
% MARKING GATES (check before every submission build):
%  1. Main content (Ch1 to end of Ch6) must be <= 60 pages: the page
%     limit is an absolute cap on the Communication criterion, which in
%     turn caps the overall mark.
%  2. Abstract must fit on one page at these margins.
%  3. Declarations (originality / ethics / GenAI) must be present and
%     in the front matter.
%  4. If _generated/robust_stats.tex is regenerated, re-read the
%     sentences coupled to specific values: "four of the ten runs reject"
%     (Ch5 5.4 pattern sentence) / "rejects in four" (Ch6 finding 3) ---
%     both assume exactly {(i), SkelWthree, SkelWfive, DirWfive} < 0.05
%     and everything else >= 0.05; "p >= \rsSpaSkelWthree across the
%     eight ... tests" (Ch1, requires SkelWthree to remain the minimum of
%     the eight skel/dir p's); and the marginal-range macro pair
%     \rsSpaSkelWthree--\rsSpaDirWfive (Ch5 x2, Ch6), which assumes
%     SkelWthree <= DirWfive.
% ------------------------------------------------------------------

% Document metadata
\title{Portfolio Optimisation with Causal Networks}
\author{Joshua Muthu}
\cid{06048741}
\supervisor{Dr.\ Ce Guo and Prof.\ Wayne Luk}
\secondmarker{Prof.\ Stephen Weston}
\date{September 2026}

\begin{document}

% --- Front matter (does not count towards the 60-page limit) ---
\pagenumbering{roman}

\maketitle

\begin{abstract}
\noindent Hierarchical portfolio allocators cluster assets on a symmetric
matrix, usually the return correlation matrix, which records co-movement
but not its direction. Temporal causal discovery
yields a richer input: a directed acyclic graph, carrying a \emph{skeleton}
(which pairs are linked) and \emph{edge orientations} (which way influence
flows); symmetric clustering keeps only the first. The thesis asks:
\emph{when the correlation matrix in hierarchical portfolio
allocation is replaced by a discovered causal graph, how much of the
performance gain is attributable to the graph's skeleton versus its edge
orientations?}

A fixed-graph ablation answers it. At 215 monthly rebalances over
2007--2024 on the ${\sim}100$ largest S\&P~500 names, one DYNOTEARS
graph (VARLiNGAM and ridge-Granger as comparators) feeds a graph-blind
HRP control, a skeleton-only allocator, and three
orientation-aware allocators that consume the DAG natively. Performance
denotes out-of-sample,
net-of-cost annualised Sharpe at four estimation horizons (nine months to
two years), adjudicated by a
PSR/DSR/Reality-Check/SPA/MCS battery over all \rsNtrials\ configurations.

The graph's total gain over the control is stable but never
individually significant ($+0.018$ to $+0.032$ net Sharpe for the best
orientation-aware allocator at every window; $p=0.43$--$0.12$), but its
composition shifts. Across the four
windows, the skeleton's rung is hump-shaped and largest near the
one-year convention, while the
orientation rung is positive at every horizon and largest at the
extremes, where sample moments are weakest. Orientation therefore acts as
a regulariser, substituting
causal structure for sample-moment quality, and attains the study's two
highest deflated Sharpe ratios. The statistical support matches this
pattern: one of
twelve pairwise orientation contrasts reaches $p<0.05$, roughly the rate
multiplicity predicts, and Hansen's SPA rejects only where the
point estimates peak: the graph family over the control at the two longest
horizons ($p=\rsSpaSkelWthree$, $\rsSpaSkelWfive$) and orientation over
the skeleton at two years ($p=\rsSpaDirWfive$), marginally; the effect
is absent under VARLiNGAM.

Two supporting results reinforce this: the HSP driver route's monthly
reward is unlearnable (${\sim}95\%$ noise) and its Sharpe varies with the
network seed; the graph route is deterministic and beats its best
seed. The
contribution is a controlled decomposition of where the
graph's value lies: in the skeleton near the one-year convention, and in
the orientations at every horizon, past the family-wise bar only at
the longest.
\end{abstract}

\chapter*{Declarations}

\section*{Statement of originality}
This report and the work it describes are my own. The research was conducted
under the supervision of Dr.\ Ce Guo and Prof.\ Wayne Luk. All external
sources (published work, datasets and third-party code) are cited in
the text; vendored third-party libraries are identified as such in the
repository. All numerical results are reproducible from the committed code
and cached data (Appendix~\ref{app:tables}).

\section*{Ethical considerations}
No Imperial ethics approval was required: the project uses only publicly
available market and macroeconomic data and involves no human participants or
personal data. The work is a retrospective backtest study; it makes no
investment recommendations, and Chapter~\ref{chap:evaluation} quantifies
the uncertainty with which its results should be read.

\section*{Use of generative AI}
Generative AI (Anthropic's Claude) was used in two capacities, both reviewed
by me as author. \emph{As a coding assistant} during implementation: all
AI-assisted code was human-reviewed and is covered by the project's test
suite and by the end-to-end verification gates of
Section~\ref{sec:ablation-design}, including a replication
gate against an independently produced historical result, so no
correctness claim rests on unverified generated code. \emph{As a drafting and
proofreading aid} for the prose of this report: AI-drafted text was checked
and edited line by line, and, because unverified numerical claims are a known
failure mode of generated text, every statistic quoted in this report is
either a macro generated directly from the committed results
(\texttt{\_generated/robust\_stats.tex}) or traceable to a committed results
file, as documented in Appendix~\ref{app:tables}. The research question,
design decisions, mathematics, experiments, analysis and conclusions are my
own.

\begin{acknowledgements}
I would like to thank my supervisors, Dr.\ Ce Guo and Prof.\ Wayne Luk, for
their guidance throughout this project, and Prof.\ Stephen Weston as my second
marker.
\end{acknowledgements}

\tableofcontents
\listoffigures
\listoftables
\clearpage

% --- Main content (60-page limit applies from here) ---
\pagenumbering{arabic}

% The spine: Ch1 states Q + R1/R2 and operationalises every term; Ch2 positions
% the three core papers against Q; Ch3 = A1 (discover the graph, C1);
% Ch4 = A2 (graph -> weights: CORR control, \skelsamp{} skeleton, \skelsem{}/\orientsamp{}/\orientsem{} orientation
% family, fixed-graph ablation design, compact HSP driver arm, C2);
% Ch5 = A3 (protocol + battery, R1, the decomposition, what survives rigour,
% threats, C3); Ch6 answers Q in two lines and lists contributions.
\input{chapters/01_introduction}
\input{chapters/02_background}
\input{chapters/03_discovery}
\input{chapters/04_sensitivity}
\input{chapters/05_evaluation}
\input{chapters/06_conclusion}

% --- Back matter (does not count towards the 60-page limit) ---
\clearpage
\printbibliography[heading=bibintoc, title={References}]

\appendix
\input{chapters/A_appendix}

\end{document}
