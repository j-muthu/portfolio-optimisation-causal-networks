# Runtime dependencies for the S&P 500 causal-discovery pipeline.
# Install into a Python 3.13 venv:
#
#     python3.13 -m venv .venv
#     .venv/bin/pip install -r pipeline/requirements.txt
#
# NOTE: causalnex and lingam are NOT installed from PyPI -- the pipeline uses
# the vendored source trees in thesis/causalnex and thesis/lingam directly
# (see pipeline/_vendored.py).  These are only their runtime dependencies.

# Core numerics / data
numpy==2.4.6
pandas==3.0.3
scipy==1.17.1
pyarrow==24.0.0          # parquet caching

# Causal discovery dependencies
networkx==3.6.1          # causalnex StructureModel
scikit-learn==1.8.0      # causalnex transformers, lingam regressions
statsmodels==0.14.6      # lingam VAR estimation
graphviz==0.21           # imported transitively by lingam.utils
semopy==2.3.11           # imported transitively by lingam.utils

# Data download
yfinance==1.3.0
lxml==6.1.1              # pandas.read_html (Wikipedia tables)
requests==2.34.2

# Analysis / parallelism
joblib==1.5.3            # rolling-window parallelism
matplotlib==3.10.9       # graph_analysis plots

# Stage 1 sensitivities: PyTorch multi-head FFNN + autograd jacobian
# Auto-uses MPS on Apple Silicon, CUDA on NVIDIA, else CPU. No CUDA toolkit
# required for the macOS / Apple-Silicon path.
torch>=2.4
# Stage 2 benchmarks: convex-optimisation min-var / Markowitz MVO
cvxpy>=1.5
# Trading-day calendar for joint matrix alignment (NYSE holidays/half-days).
pandas-market-calendars>=4.4
# WRDS (CRSP) primary asset price source. We connect directly to WRDS's
# PostgreSQL endpoint via SQLAlchemy + psycopg (not the `wrds` Python library,
# which pins old pandas/numpy and conflicts with this project's pins). Auth
# is via ~/.pgpass — same libpq convention the wrds library uses internally.
# See pipeline/data/wrds_backend.py.
sqlalchemy>=2.0
psycopg[binary]>=3.1
# Excel I/O for the Rodriguez-Dominguez HSP reference dataset (Phase E.2).
openpyxl>=3.1
